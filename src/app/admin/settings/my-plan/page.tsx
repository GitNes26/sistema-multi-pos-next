import {getServerSession}from"next-auth";import{redirect}from"next/navigation";import{authOptions}from"@/lib/auth/options";import{effectiveOrgId}from"@/lib/auth/org-context";import{subscriptionUsage}from"@/lib/billing/subscriptions";import{prisma}from"@/lib/db";import{PageHeader}from"@/components/layout/page-header";import{Card,CardContent}from"@/components/ui/card";import{BadgeDollarSign,Building2,Users,Check}from"lucide-react";import{Button}from"@/components/ui/button";import Link from"next/link";
export default async function Page(){const s=await getServerSession(authOptions);const id=effectiveOrgId(s);if(!id)redirect("/admin");const[usage,plans]=await Promise.all([subscriptionUsage(id),prisma.subscriptionPlan.findMany({where:{isActive:true},orderBy:{monthlyPrice:"asc"}})]);const sub=usage.subscription;return <div className="space-y-5"><PageHeader icon={<BadgeDollarSign/>} title="Mi plan" description="Consulta tu capacidad actual y compara las opciones disponibles."/><Card><CardContent className="grid gap-4 p-5 md:grid-cols-3"><div><p className="text-sm text-muted-foreground">Plan actual</p><p className="text-2xl font-semibold">{sub?.plan.name??"Sin plan asignado"}</p><p className="text-sm text-muted-foreground">{sub?`Vence ${new Date(sub.periodEndsAt).toLocaleDateString("es-MX")}`:"Contacta al administrador del sistema"}</p></div><div><p className="flex gap-2 font-medium"><Building2 className="size-5"/>Sucursales</p><p className="text-2xl font-semibold">{usage.locations} / {usage.locationLimit??"—"}</p></div><div><p className="flex gap-2 font-medium"><Users className="size-5"/>Empleados</p><p className="text-2xl font-semibold">{usage.employees} / {usage.employeeLimit??"—"}</p></div></CardContent></Card><div className="grid gap-4 lg:grid-cols-3">{plans.map(p=><Card key={p.id}><CardContent className="space-y-4 p-5"><h2 className="text-xl font-semibold">{p.name}</h2><p className="text-3xl font-semibold">${Number(p.monthlyPrice).toLocaleString("es-MX")}<span className="text-sm font-normal text-muted-foreground">/mes</span></p><p className="text-sm">{p.includedLocations} sucursal(es) · {p.includedEmployees} empleados</p><ul className="space-y-2 text-sm">{(p.features as string[]).map(f=><li key={f} className="flex gap-2"><Check className="size-4 text-primary"/>{f}</li>)}</ul><Button asChild variant={sub?.planId===p.id?"secondary":"default"} className="w-full"><Link href="mailto:soporte@multi-pos.local?subject=Actualización de plan">{sub?.planId===p.id?"Plan actual":"Solicitar actualización"}</Link></Button></CardContent></Card>)}</div></div>}
